{% extends 'base.html.twig' %}

{% block title %}{{ title }} | FinApp{% endblock %}

{% block main %}
    <div class="mb-4">
        <div class="page-kicker">Phase 1 shell</div>
        <h1 class="h2 mb-0">{{ title }}</h1>
    </div>

    <div class="card">
        <div class="card-body">
            <p class="text-secondary mb-0">This protected section is ready for the next implementation phase.</p>
        </div>
    </div>
{% endblock %}
